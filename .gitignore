# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual Environment
.venv/
venv/
env/

# Local environment overrides
.env
.env.local

# Database
*.db
*.sqlite3
!data/seed/*.db
# But not the timestamped backups that promote_to_seed.py creates
data/seed/*.bak.db

# Output
*.csv
scrape_log.txt

# Tooling
.claude/

# OS generated files
.DS_Store
Thumbs.db

# Debug dumps
*.html
*.json
!data/reference/*.json
!association_neighborhoods.json
!neighborhood_incomes.json
!calgary_2021_census_incomes.json
# Node project manifests live in web/ — must be tracked or `npm ci` can't run.
!web/package.json
!web/package-lock.json
!web/tsconfig.json
